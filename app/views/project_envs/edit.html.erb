<% content_for :title, "Edit #{@project_env.qualified_name}" %>
<% lede = page_lede("hints.pages.project_envs_edit.intro") %>
<h1 class="page-title <%= lede ? "mb-1" : "mb-6" %>">Edit environment: <span class="font-mono"><%= @project_env.qualified_name %></span></h1>
<% if lede %><p class="text-sm mb-6 text-ink-soft"><%= lede %></p><% end %>
<%= render "form", project_env: @project_env %>
