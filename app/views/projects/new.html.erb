<% content_for :title, "New project" %>
<h1 class="page-title mb-1">New project</h1>
<p class="text-sm mb-6 text-ink-soft"><%= t("hints.pages.projects_new.intro") %></p>
<%= render "form", project: @project %>
