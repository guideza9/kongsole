<% content_for :title, "New project" %>
<% lede = page_lede("hints.pages.projects_new.intro") %>
<h1 class="page-title <%= lede ? "mb-1" : "mb-6" %>">New project</h1>
<% if lede %><p class="text-sm mb-6 text-ink-soft"><%= lede %></p><% end %>
<%= render "form", project: @project %>
