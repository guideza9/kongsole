<% content_for :title, "Edit #{@project.name}" %>
<% lede = page_lede("hints.pages.projects_edit.intro") %>
<h1 class="page-title <%= lede ? "mb-1" : "mb-6" %>">Edit project: <%= @project.name %></h1>
<% if lede %><p class="text-sm mb-6 text-ink-soft"><%= lede %></p><% end %>
<%= render "form", project: @project %>
