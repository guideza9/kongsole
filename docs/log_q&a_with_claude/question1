คำถามทั้งหมด (เรียงตามผลกระทบ)
ข้อที่มีคำแนะนำ ตอบว่า "ตามแนะนำ" ได้

1.token หลุด: คุณจะ revoke PAT kctl_b27f… ที่หน้า Tokens เองไหม และต้องการ rewrite ประวัติ git หรือแค่แก้ไฟล์ในงานถัดไป (⭐ revoke + แก้ไฟล์ + ไม่ rewrite ประวัติ เพราะ token ใช้ได้เฉพาะ Kongsole ในเครื่อง)
2.Redactor ที่ตกหล่น: ให้แก้เป็น task แรกสุดก่อน R1 ไหม (⭐ ใช่ เพราะผิดกฎข้อ 4 อยู่ตอนนี้)
3.ที่มาของ rank และ apply_mode: ให้กำหนดต่อ env ใน connections.yml (git) เท่านั้นและแก้ใน UI ไม่ได้ไหม (⭐ ใช่ เพราะเป็น input ของ guardrail)
4.ค่าจริงของ ProjectA และ ProjectX (R1 Q1–2): rank และ apply_mode ของแต่ละ env โดยเฉพาะ pt และ ps
5.env เดียวมีหลาย node (R1 Q3): ถ้ามี ให้เขียนผ่าน node ไหน (⭐ node เดียวเป็น primary ส่วนตัวอื่นอ่านอย่างเดียว)
6.config repo (R1 Q4): repo แยกต่อ project หรือ repo เดียว
7.การสลับ env: เลือกแบบไหนจากหัวข้อ 5 (⭐ แบบที่สอง) และสีประจำ connection กำหนดต่อ env หรือต่อ rank (R1 Q5) (⭐ ความ "ดัง" ของ UI ตาม rank ส่วนสีจุดตาม env)
8.ชื่อที่ MCP ใช้อ้าง: ใช้รูปแบบ project/env (และ /node ถ้ามีหลาย node) ไหม
9.git host (R8 Q1): ถ้ายังไม่เลือก จะยอมให้รอบนี้แค่ "push branch + ลิงก์ compare" ก่อนไหม
10.ขอบเขต changeset (R8 Q2): หนึ่ง changeset ต่อหนึ่ง connection ไหม (⭐ ใช่)
11.จำนวนการลบที่ block (R8 Q3): CiGate ตอนนี้ใช้ 3 จะใช้ค่านี้หรือค่าอื่น
12.changeset ใน direct mode (R8 Q4): (⭐ ไม่ทำในรอบนี้)
13.MCP กับ changeset/การสร้าง (R2 Q1, R8 Q5): ตอนนี้ MCP สร้าง service/route ได้อยู่แล้ว จะจำกัดเพิ่มไหม (⭐ agent เพิ่มรายการเข้า changeset ได้ แต่ส่ง PR ได้เฉพาะคน)
14.plan เดี่ยวของ PR mode: ให้ทุกการเขียนใน PR mode เข้า changeset (plan เดี่ยวกลายเป็น changeset ขนาด 1) ไหม (⭐ ใช่)
15.ภาษาของ hint (R3 Q1): ไทยโดยคงศัพท์เทคนิคเป็นอังกฤษไหม และย้ายทุกข้อความของ UI เข้า locale หรือเฉพาะ hint
16.เครือข่าย (R3 Q3): เข้า Google Fonts และเอกสารของ Kong ได้ไหม ถ้าไม่ได้ผมจะ bundle ฟอนต์ไทย (เช่น IBM Plex Sans Thai) มากับแอป
17.คนเขียนและตรวจ hint (R3 Q2): ใครเขียน ใครตรวจ และใครเป็นคนทดสอบจริงที่ไม่เคยใช้ Kong
18.รูปแบบฟอร์ม R3/R4: เลือกแบบไหนจากหัวข้อ 5 (⭐ แบบแรก)
19.R4:
- คำอธิบายของ custom plugin มาจากไหน (⭐ ไฟล์ metadata ใน repo Kongsole)
- แสดง plugin ที่ไม่ได้โหลดบน node ไหม (⭐ ไม่แสดง)
- เตือนเมื่อ schema ต่างกันข้าม connection ไหม (⭐ เตือน)
20.R2:
- ต้องมีค่าตั้งต้นต่อ project ไหม (⭐ ยังไม่ทำ)
- flow promote ในรอบนี้ไหม (⭐ ไม่ทำ)
21.นิยาม "route ซ้อน": (⭐ เตือนเมื่อ host ทับกันและ path ซ้ำหรือเป็น prefix ของกัน ส่วน regex บอกว่า "ตรวจไม่ได้")
22.R5: เลือกทางเลือกไหน และเก็บข้อมูลที่ทีมเขียนไว้ที่ใด (⭐ ผสม + repo Kongsole)
23.R6:
- เลือกทางเลือกไหน และยังต้องใช้ File Log อยู่ไหม
- ค่า N และ M
- ต้องการใน env ไหน และยอมเพิ่ม plugin บน prod ไหม
- องค์กรมี Prometheus หรือ log pipeline อยู่แล้วไหม
- latency เพิ่มได้ไม่เกินเท่าไร
24.R7:
- export ไปใช้ทำอะไร
- ยืนยันความหมายของ template (⭐ filter + แทนค่าตาม env)
- export จาก direct env ได้ไหม (⭐ ได้ พร้อม header)
- kong_export ของ MCP อยู่ใน scope ไหม
25. DESIGN.md: อนุญาตให้ผมร่างข้อความแก้ §3/§12/§15 ไว้ใน docs/plans/ แล้วให้ทีมเป็นคนแก้ไฟล์จริงไหม
26. สภาพแวดล้อมทดสอบ: คุณจะเปิด Docker และ Postgres ให้ไหม และขอเพิ่ม task แรกใน plan ให้ test ใช้ encryption key เฉพาะ test เพื่อให้ baseline ไม่มีข้อที่ล้ม
27. ลำดับ requirement ที่ร่างไว้: R3 (โครงสร้างกลาง) → R1 → R8 → R2 → R4 → R5 → R7 → R6 เห็นด้วยไหม