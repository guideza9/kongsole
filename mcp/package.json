{
  "name": "kongctl-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MCP stdio server for the Kong CE control plane (docs/DESIGN.md section 12) -- a thin client over the PAT-secured /api/v1 REST API.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.11"
  }
}
